<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cyz.android.hospital">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/c"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/c"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".action.MainActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".action.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".action.AddHoregister"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".action.DetailsHoregister" />
        <activity
            android:name=".action.UpdateHoregister"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".action.DrawerActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustUnspecified|stateHidden"></activity>
    </application>

</manifest>